export { compressImage } from './compress-image'
export type { CompressOptions } from './compress-image'

/** Suggested max dimension for avatars (small, square, re-encoded as WebP). */
export const AVATAR_MAX_DIMENSION = 512

/** Suggested max dimension for posters, event images, and story photos. */
export const POSTER_MAX_DIMENSION = 1920
